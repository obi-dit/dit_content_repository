export enum ContentType {
  ARTICLE = "article",
  DOCUMENTATION = "documentation",
  GUIDE = "guide",
  VIDEO = "video",
  AUDIO = "audio",
  IMAGE = "image",
  PDF = "pdf",
  WORD = "word",
  EXCEL = "excel",
  POWERPOINT = "powerpoint",
}

export enum ContentStatus {
  DRAFT = "draft",
  PUBLISHED = "published",
  ARCHIVED = "archived",
}

export enum ContentAuthor {
  USER = "user",
  ADMIN = "admin",
}

export enum UserType {
  ADMIN = "admin",
  USER = "user",
  COMPANY_USER = "company_user",
}
